let fruits = ['mango', 'papaya', 'pineapple', 'apple'];

// Iterate over fruits below
fruits.forEach(function(x){
  console.log('I want to eat a ' + x);
});

fruits.forEach(x =>
    console.log('I want to eat a ' + x)
  );
